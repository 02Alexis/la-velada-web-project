<div
  class={`-to-1/2 -left-1/2 -bottom-1/2 -right-1/2 fixed bg-repeat bg-transparent w-[200%] h-[200vh] inset-0 z-[99999] pointer-events-none contrast-125 brightness-150`}
>
</div>

<style>
  div {
    background-image: url("./noise.png");
    animation: noise 0.2s infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    div {
      animation: none;
    }
  }

  @keyframes noise {
    o% {
      transform: translate(0, 0);
    }
    10% {
      transform: translate(-5%, -5%);
    }
    20% {
      transform: translate(-0%, -5%);
    }
    30% {
      transform: translate(-0%, -5%);
    }
    40% {
      transform: translate(-0%, -5%);
    }
  }
</style>
